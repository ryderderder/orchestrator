# Install teamctl via your AI agent

Paste the block below into any AI coding CLI — Claude Code, Codex, Grok,
Gemini, or similar. The prompt is a pointer, not a manual: the repo's own
docs carry the instructions, and your agent reads them first.

```text
Install teamctl for me from this repo:
https://github.com/ryderderder/teamctl
Orient first — read README.md, docs/AGENT_GUIDE.md, and
llms.txt from the repo. Then install, configure, and
verify it yourself autonomously (teamctl doctor).
Ask me before anything that needs sudo.
```
